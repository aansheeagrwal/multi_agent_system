def process_json(content: str, follow_up: bool = False):
    """
    Process JSON content.
    """
    # Your JSON processing logic here
    return {
        "message": "JSON processed",
        "content_length": len(content),
        "follow_up": follow_up
    }
